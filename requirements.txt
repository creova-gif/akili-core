# ============================================================
# AKILI AI — Full Requirements (Phase 5)
# ============================================================

# Core
anthropic>=0.40.0
python-telegram-bot>=21.0
aiohttp>=3.9.0
schedule>=1.2.0
python-dotenv>=1.0.0

# Twitter/X
tweepy>=4.14.0

# Google (Gmail — both accounts)
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# Phase 5 — SHIELD system health
psutil>=5.9.0

# Phase 5 — PULSE image generation (optional — add OPENAI_API_KEY to unlock)
openai>=2.46.0

# Utilities
httpx>=0.25.0
